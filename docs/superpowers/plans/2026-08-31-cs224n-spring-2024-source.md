# CS224n Spring 2024 Source Update Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Pin stage two's CS224n recommendation to the selected Spring 2024 Bilibili collection and its actual L7-L11 topic sequence.

**Architecture:** This is a documentation-only replacement in the roadmap's resource summary and detailed outline. Keep the two descriptions synchronized and verify that no generic Stanford URL or stale lecture range remains.

**Tech Stack:** Markdown, ripgrep, Git

---

### Task 1: Update the CS224n source and selected lectures

**Files:**
- Modify: `docs/EXTERNAL_FREE_BILINGUAL_AI_INFRA_ROADMAP.md:238`
- Modify: `docs/EXTERNAL_FREE_BILINGUAL_AI_INFRA_ROADMAP.md:255`

- [ ] **Step 1: Confirm the stale source and lecture range are present**

Run:

```bash
rg -n "web.stanford.edu/class/cs224n|L10–L13、L15" docs/EXTERNAL_FREE_BILINGUAL_AI_INFRA_ROADMAP.md
```

Expected: matches in the stage-two resource summary and CS224n outline.

- [ ] **Step 2: Replace the resource summary**

Use this exact summary:

```markdown
- **Stanford CS224n Spring 2024**（EN / University，固定使用此 B 站合集；只取 **L7–L11**：Attention / LLM Intro、Self-Attention / Transformer、Pretraining、Post-training、NLG；其余讲次跳过）：[B站 BV163Jc6pENx](https://www.bilibili.com/video/BV163Jc6pENx/)。
```

- [ ] **Step 3: Replace the detailed outline**

Use this exact outline:

```markdown
- **CS224n Spring 2024** 大纲（固定使用上述 B 站合集，只取 **L7–L11**，其余跳过）：
  - ★ L7 Attention、LLM Introduction
  - ★ L8 Self-Attention and Transformers
  - ★ L9 Pretraining
  - ★ L10 Post-training
  - ★ L11 Natural Language Generation（衔接阶段八的 LLM 原理）
  - L1–L6、L12–L19 与 Python / PyTorch / Hugging Face Tutorial：全部跳过
```

- [ ] **Step 4: Verify the new source and selection**

Run:

```bash
rg -n "CS224n Spring 2024|BV163Jc6pENx|L7–L11" docs/EXTERNAL_FREE_BILINGUAL_AI_INFRA_ROADMAP.md
```

Expected: the summary and outline consistently identify Spring 2024 and L7-L11, and the summary contains the selected Bilibili ID.

Run:

```bash
if rg -n "web.stanford.edu/class/cs224n|L10–L13、L15" docs/EXTERNAL_FREE_BILINGUAL_AI_INFRA_ROADMAP.md; then exit 1; fi
```

Expected: exit status 0 with no output.

- [ ] **Step 5: Check the Markdown diff and commit**

Run:

```bash
git diff --check -- docs/EXTERNAL_FREE_BILINGUAL_AI_INFRA_ROADMAP.md
git diff -- docs/EXTERNAL_FREE_BILINGUAL_AI_INFRA_ROADMAP.md
```

Expected: no whitespace errors and only the two approved CS224n blocks are changed.

Commit:

```bash
git add docs/EXTERNAL_FREE_BILINGUAL_AI_INFRA_ROADMAP.md
git commit -m "docs: pin CS224n Spring 2024 course"
```
